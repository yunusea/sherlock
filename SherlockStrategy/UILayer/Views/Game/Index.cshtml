
<div class="col-md-12">
    <h1>Oyunlar</h1>
    <table class="table table-hover">
        <tr>
            <td>Oyun Adı</td>
            <td>İşlem</td>
        </tr>
        <tr ng-repeat="game in GetGameList">
            <td>{{game.GameName}}</td>
            <td><input type="submit" class="btn btn-default" ng-click="GotoGame(game.Id)" value="Yeni Oyun" /> <input type="submit" class="btn btn-default" ng-click="GotoOngoingGameList()" value="Devam Eden Oyunlar" /></td>
        </tr>
    </table>

</div>